# Harness Engineering 与 oh-my-xx：从模型能力到编排能力

> **分享日期**：2026年4月1日
>
> **分享人**：ChatGPT × OpenCode
> **关键词**：Harness Engineering、Agent Orchestration、任务分类路由、专职代理、验证闭环、Fallback

---

## 开篇：为什么今天真正稀缺的，不再只是更强的模型

这两年，大家聊 AI Agent，最容易陷进去的一个讨论方式，是把焦点全部放在模型身上：谁的推理更强，谁的代码能力更好，谁的上下文窗口更长，谁更像一个“全能选手”。

但只要真正把 Agent 放进生产环境，你很快就会发现一件事：**模型能力决定上限，编排能力决定下限，而真实交付往往输在下限。**

一个模型可能会推理、会写代码、会调用工具，但它仍然会出现这些问题：

- 上下文读多了之后开始发散；
- 对任务理解不稳定，容易把“先分析”做成“直接开改”；
- 知道大方向，却在落地时选错工具、改错文件、漏掉验证；
- 在一个坏模式上不断复用，把偶发问题放大成系统性问题；
- 遇到失败时不会自然收敛，而是进入“重复编辑—重复失败”的循环。

所以今天再看 Agent，核心问题已经不是“模型到底够不够聪明”，而是：**我们有没有给它一个足够好的 Harness。**

本文不再重复“什么是 Agent”的基础入门，而是聚焦下面几个问题：

1. **Harness Engineering 到底在解决什么问题？**
   → 它不是提示词技巧的升级版，而是让 Agent 稳定交付的运行时工程。
2. **为什么 oh-my-xx 这种模式，会自然长成多角色、强路由、重验证的样子？**
   → 因为单点智能不够，必须把智能组织起来。
3. **一个好的编排系统，到底要管哪些事情？**
   → 不只是“让模型会做事”，更是“让模型在对的边界里做对的事”。
4. **为什么说 AI 的竞争焦点，正在从模型能力转向编排能力？**
   → 因为用户真正买单的不是回答，而是完成率、稳定性、可治理性和可持续性。
5. **这条路最大的机会和最大的问题分别是什么？**
   → 前者是把个人效率放大成系统效率，后者是把系统复杂度误当成智能。

**大纲速览**：

第一部分：从模型崇拜到编排崇拜 ─── 为什么 Agent 问题本质上是系统问题  
第二部分：Harness Engineering 的核心任务 ─── 解释、约束、路由、验证、治理  
第三部分：oh-my-xx 为什么像一个“AI 组织” ─── 分类器、专职代理、并行执行、Fallback  
第四部分：把抽象概念翻译成真实工作流 ─── 从用户一句话到可验证交付  
第五部分：价值、代价与边界 ─── 什么时候它是护城河，什么时候它会变成负担  
总结与展望 ─────────────────── 从单点模型时代，走向系统智能时代

**推荐阅读路径**：

- **只想抓主结论**：开篇 → 第二部分 → 第五部分 → 总结与展望
- **关注系统设计**：第一部分 → 第二部分 → 第三部分 → 第五部分
- **关注实操方法**：第三部分 → 第四部分 → 总结与展望
- **关注产品判断**：开篇 → 第一部分 → 第五部分

---

## 第一部分：从模型崇拜到编排崇拜

### 1.1 模型更强了，为什么问题反而更“工程化”了？

一个很容易被忽略的事实是：当模型弱的时候，大家讨论的是“它能不能做”；当模型变强之后，大家真正头痛的是“它为什么老是做偏”。

这背后其实是一个很典型的系统工程现象。

过去做 Prompt Engineering，大家关注的是：

- 我该怎么说，模型才会更懂；
- 我要不要给示例；
- 角色设定怎么写；
- 输出格式怎么约束。

后来大家转向 Context Engineering，关注点变成：

- 模型该看到哪些文件；
- 要不要引入知识库；
- 工具怎么接；
- 记忆怎样保留。

再往前走一步，进入 Harness Engineering，问题就变成了：

- 整个系统应该怎么运作，模型才不容易偏航；
- 任务应该如何分类和路由；
- 哪些阶段必须自动验证；
- 失败后是重试、降级、换路径还是回退；
- 用户、工具、记忆、权限和审计要如何放在同一个运行时里。

换句话说，**Prompt Engineering 关注“怎么说”，Context Engineering 关注“给它看什么”，Harness Engineering 关注“整个环境怎么让它稳定干活”。**

这也是 OpenAI 在 Harness Engineering 文章里真正点出来的东西：难点越来越不在模型本身，而在模型周围那一圈系统设计。Anthropic 在 Building Effective Agents 里给出的建议也是类似的——先从最简单、最可靠的模式开始，把复杂度加在真正需要的地方。

### 1.2 Harness 不是提示词的高级写法，而是运行时纪律

“Harness” 这个词本来就带有很强的物理感：缰绳、马鞍、护具、控制结构。它默认承认一个事实——**被驾驭的对象是有力量的，但它本身并不天然可靠。**

放到 Agent 世界里，这句话可以翻译成：

> 模型具备能力，但不天然具备纪律。

因此，一个真正有效的 Harness，至少要处理五件事：

1. **解释意图**：把用户的自然语言需求，翻译成正确的任务类型。  
   例如“帮我看看”通常意味着调查，“帮我修”通常意味着改动，“你怎么看”通常意味着评估而不是执行。
2. **提供边界**：明确工具权限、任务范围、可变更区域、不可逆动作。  
   例如可以读代码，但不能默认推送远端；可以修改某几个文件，但不能擅自重构整个模块。
3. **建立路由**：把不同任务交给不同能力单元，而不是所有事都丢给同一个上下文窗口。  
   例如研究交给研究代理，视觉任务交给视觉代理，架构评估交给高质量审查代理。
4. **形成闭环**：执行之后必须有验证、失败之后必须有反馈。  
   没有验证的 Agent，不叫交付系统，只能叫自动生成器。
5. **治理复杂度**：控制上下文污染、模型退化、错误扩散、Token 成本和权限风险。  
   这部分看起来“最不性感”，却往往是系统成败分水岭。

所以我更愿意把 Harness Engineering 理解为一句话：

**它是把 AI 从“会回答”变成“能交付”的那层运行时纪律。**

### 1.3 为什么说竞争点正在从模型能力，转向编排能力

因为用户最后感知到的，从来不是“这个模型平均推理分数更高”，而是下面这些东西：

- 任务到底能不能完成；
- 完成后有没有副作用；
- 出错时能不能快速回到正轨；
- 对长任务是否会越来越笨；
- 系统能不能解释自己在做什么；
- 出现问题之后，团队能不能审计、定位和修复。

如果说模型能力像发动机，那 Harness 更像整车底盘。发动机当然重要，但真正决定这辆车能不能稳定跑长途的，往往是传动、制动、悬挂和安全系统。

所以，“从模型崇拜走向编排崇拜”并不意味着模型不重要，而是意味着：**当模型能力逐渐拉平后，组织这些能力的方式，开始成为更大的差异化来源。**

---

## 第二部分：Harness Engineering 到底在管什么

### 2.1 一个可交付的 Agent 系统，至少要有一条完整流水线

如果把一个成熟的 Agent 系统抽象成流程，它大致会长这样：

```text
用户请求
  ↓
意图识别 / 风险判断
  ↓
上下文收集 / 模式搜索
  ↓
任务分类 / 路由分发
  ↓
执行 / 子任务并行
  ↓
验证 / 失败反馈 / 重试
  ↓
输出结果 / 保留记忆 / 写入审计
```

这条流水线里，模型只是其中一个核心部件，而不是全部。

一个系统如果只有“模型 + 工具调用”，但没有：

- 分类器，
- 验证器，
- 失败反馈，
- 权限控制，
- 成本和上下文管理，

那它更像一个会自动调用 API 的实验品，而不是一个可以长期使用的 Agent 运行时。

### 2.2 解释层：先把一句话理解对，后面才有资格谈执行

用户需求在进入系统的第一步，其实不是“开始做”，而是“先判断这到底是什么任务”。

这一步经常被低估，但实际上非常关键。因为下面这些表达，虽然表面接近，系统动作却完全不同：

- “你帮我看看这个问题” → 调查型任务
- “你帮我加这个功能” → 实现型任务
- “你怎么看这个设计” → 评估型任务
- “帮我整理成文档” → 写作型任务
- “这个页面不对劲” → 诊断型任务

如果系统在这一层判断错了，后面的所有聪明都会变成南辕北辙。

所以 Harness 的第一职责，不是让模型“开始思考”，而是让系统先问自己：

> 这到底是分析任务、实施任务、写作任务，还是需要提问澄清的任务？

### 2.3 路由层：把合适的任务，交给合适的心智单元

oh-my-xx 这类系统最有代表性的一个特征，就是它不再假设“一个 Agent 干所有事”是最佳方案。

它更像一个组织，而不是一个人。

这里面至少有三层思路：

#### 第一层：任务先分类

不是所有任务都值得用同一种推理强度，也不是所有任务都要同一种上下文密度。常见的分类维度包括：

- 视觉 / 交互类
- 逻辑 / 架构类
- 研究 / 调研类
- 写作 / 总结类
- Git / 交付类
- 简单快修类

这件事看上去像“产品分流”，但本质上是在做**认知预算分配**。

#### 第二层：角色要专职化

一个擅长搜资料的代理，不应该同时承担高风险改动；一个擅长架构审查的代理，也不适合拿去做琐碎的文件修订。

所以，专职代理不是为了“看起来像多 Agent”，而是为了把不同类型的认知工作拆开，避免在一个上下文里互相污染。

#### 第三层：父代理负责编排，子代理负责隔离

这类系统里最重要的不是代理数量，而是上下文边界。

主代理如果既要规划、又要搜索、又要读几十个文件、又要自己写、又要做验证，它的上下文很快就会变脏。真正稳的做法是：

- 主代理保持问题视角和任务全局；
- 子代理进入局部上下文执行具体调查；
- 返回的是压缩结论和引用，而不是整段噪声。

这件事在本质上是一个“上下文防火墙”。

### 2.4 验证层：成功应该静默，失败必须可操作

很多人讨论 Agent 时，最容易只盯着“会不会做”，却忽略了“做完怎么知道对不对”。

这恰恰是 Harness 最关键的部分之一。

一个成熟的验证层至少要做到：

- 能区分“生成完成”和“任务完成”；
- 能把成功信号压缩到足够短，不污染上下文；
- 能把失败信号压缩成最小可操作单元；
- 能知道当前问题是继续重试，还是换方法更合适。

所以一个好的 Agent 系统不会满足于“模型说它做好了”，而会继续追问：

- 类型检查过了吗？
- 构建过了吗？
- 相关测试跑了吗？
- 文档与需求是否对应？
- 高风险动作是否经过确认？

**验证不是结尾动作，而是编排系统的内建结构。**

### 2.5 治理层：真正难的不是让它做事，而是让它长期不变形

当 Agent 系统从一次性 Demo 进入日常使用，治理问题会迅速冒出来：

- 上下文越滚越长，效果越来越差；
- 一次错误模式被复制成十次；
- 成本失控；
- 模型偶发失败导致整个流程中断；
- 用户无法知道系统到底做了什么；
- 外部动作缺乏审计和可追溯性。

这也是为什么很多看上去“很智能”的 Agent，一旦进入团队协作环境就突然变得不实用。因为真正长期可用的，不只是智能水平，而是：

- 是否有明确权限边界；
- 是否有失败后的回退路径；
- 是否支持模型降级和 Fallback；
- 是否保留可审计的操作历史；
- 是否能控制上下文和技术债扩散。

换句话说，**Harness 真正处理的是“智能如何被组织起来”，而不是“智能是否存在”。**

---

## 第三部分：为什么 oh-my-xx 会自然长成这种样子

### 3.1 从单点智能到系统智能

很多人第一次看到 oh-my-openagent / oh-my-opencode 这类系统时，会觉得它们“有点复杂”：

- 为什么要分类？
- 为什么要多个角色？
- 为什么要 background tasks？
- 为什么要先探索、再规划、再验证？
- 为什么明明能直接做，还要引入那么多约束？

如果只从“让模型尽快回答”这个目标看，这些设计确实显得重。

但如果目标从“尽快给一个回答”变成“稳定完成一个非平凡任务”，这些设计就会显得非常自然。

因为 oh-my-xx 这类系统，本质上不再把 AI 当作一个对话机器人，而是把它当成一个**可编排的数字化工作系统**。

### 3.2 任务分类路由：不是形式主义，而是成本与正确率管理

一个典型的 oh-my-xx 系统，往往会先做任务分类：

- trivial / quick 类交给轻量模型或轻量路径；
- research / explore 类交给调查型代理；
- visual 类交给专门的视觉工程代理；
- architecture / hard logic 类交给更强的高推理代理；
- writing 类交给写作型代理。

这背后至少解决了三件事：

1. **成本匹配**：不是所有任务都值得用最贵的模型。
2. **上下文匹配**：不是所有任务都需要看完整上下文。
3. **失败隔离**：一个调查子任务出错，不应该污染主任务的全局判断。

所以分类路由并不是“复杂系统爱好者的仪式感”，而是编排系统必须学会的第一种节制。

### 3.3 专职代理：把“会一点很多事”，变成“每个环节都有人负责”

如果说传统 Prompt 更像在教一个人如何更聪明地回答问题，那么专职代理更像是在搭一个小团队。

这个团队里常见的角色包括：

- **Explore / Librarian**：负责搜集信息、定位模式、理解现状；
- **Oracle**：负责高质量判断、架构评估、复杂问题复核；
- **Writer / Reviewer**：负责把结果写成可消费的文档；
- **Executor**：负责在约束下完成改动和交付；
- **Verifier**：负责检查它是不是真的完成了，而不是“声称完成了”。

你会发现，这套结构和真实团队协作是有映射关系的。

这不是偶然。因为当任务复杂度超过单一步骤，系统最自然的进化方式，就是从“单人全能幻觉”走向“角色分工 + 协调”。

### 3.4 并行执行：吞吐量真正来自“同时推进不同认知任务”

oh-my-xx 这一类系统还强调一件事：**能并行的，就尽量不要串行。**

这个并行，不只是技术上的“同时发几个请求”，而是工作流上的并行：

- 一边查代码结构，一边查外部文档；
- 一边看本地模式，一边看远端资料；
- 一边做局部探索，一边准备验证；
- 一边让子代理查证，一边主代理保持全局思路。

这其实很接近现实中的高效工作方式：

> 真正高效的人，不是把事情一件件排成队，而是知道哪些认知动作可以同时发生。

这也是为什么很多 Agent 看起来“能做”，却整体吞吐量不高。因为它们本质上仍然是单线程对话机器人，只是多了几个工具而已。

### 3.5 Fallback 和治理：韧性来自系统，不来自运气

oh-my-xx 这类模式还有一个很重要但容易被忽视的点：**它把失败当成常态来设计，而不是当成偶发来祈祷。**

所以你会看到这些机制：

- 主模型失败时切到备用模型；
- 搜索无结果时换检索策略；
- 一个子任务失败时，不拖垮整个主任务；
- 高风险动作必须显式确认；
- 有问题先停下来，先验证、再继续。

这说明它追求的不是“每次都完美”，而是“即便不完美，也能维持可恢复性”。

而这正是工程系统和 Demo 系统最大的差别。

---

## 第四部分：把抽象概念翻译成真实工作流

### 4.1 一个成熟 Harness 的工作方式，大概长什么样

如果把上面的概念压缩成一条更贴近日常协作的流程，它通常会是：

```text
理解任务
  ↓
判断类型（分析 / 实施 / 写作 / 评估）
  ↓
检查是否缺关键信息
  ↓
探索上下文（代码 / 文档 / 外部资料）
  ↓
规划修改点或输出结构
  ↓
选择执行路径（自己做 / 委派 / 并行）
  ↓
执行
  ↓
验证
  ↓
失败重试或换路
  ↓
输出结果并保留审计痕迹
```

这条流程看起来很像一个严谨工程师的工作方法。其实这也正是 Harness Engineering 最值得注意的地方：

**它不是在模仿“一个更会说话的模型”，而是在模仿“一个更像工程系统的工作过程”。**

### 4.2 从用户视角看，感知到的其实只有三件事

虽然系统内部可能做了很多复杂动作，但从用户视角，真正感知到的通常只有三件事：

#### 第一，它是不是理解了我的真实意图

例如：

- 我只是想让它分析，它却直接改代码；
- 我明明要落库，它却只给了一段文本；
- 我只是要一个结论，它却开始铺很长的技术细节。

如果这一层经常错，用户不会觉得系统“聪明”，只会觉得系统“自作主张”。

#### 第二，它是不是能把复杂任务拆得足够稳

一个真正能用的系统，不是上来就开始“全自动表演”，而是会自然形成这样一种感觉：

- 它知道先看什么；
- 它知道什么该并行查；
- 它知道什么时候应该停下来问一句；
- 它知道什么时候必须做验证。

这就是用户能直接感知到的“稳”。

#### 第三，它是不是能对结果负责

最后用户真正买单的是：

- 文档有没有写对；
- 代码有没有写进仓库；
- 提交有没有成功；
- 出了问题能不能说明白。

所以从体验层面看，Harness 并不是抽象概念，而是“一个系统是否值得继续信任”的核心来源。

### 4.3 一个具体例子：为什么“写文章”也需要 Harness

以这次任务为例，表面上看只是：

> 参考一篇文章的写法，再写一篇类似风格的文章，上传到 GitHub。

但如果按 Harness 的思路拆开，它其实包含了多个动作：

1. 确认参考文章到底是哪一篇，以及它的风格特征；
2. 研究 Harness Engineering 和 oh-my-xx 的核心观点，而不是只做表面模仿；
3. 判断新稿应该写成“综述型”还是“方法论 essay”；
4. 同步远端仓库，避免在旧版本上落稿；
5. 写入新文件；
6. 验证内容和仓库状态；
7. 提交并推送；
8. 再由高质量审查角色复核文章 framing 是否跑偏。

如果缺少其中任何一环，最后的结果都可能出问题：

- 风格像了，但内容空；
- 内容有了，但没有落库；
- 文档写进去了，但覆盖了远端；
- 文章表面很像，实质只是概念包装。

这也说明：**Harness 并不只服务于代码任务，它服务于一切“需要多步交付和验证”的任务。**

---

## 第五部分：价值、代价与边界

### 5.1 这种模式真正值钱的地方是什么

如果把 Harness Engineering 和 oh-my-xx 这种模式放在一起看，它真正值钱的地方至少有四个：

#### 第一，提升的不是单次回答质量，而是整体完成率

单次回答好，不代表任务能做完。真正有价值的是：

- 任务是否走得完；
- 过程中是否少偏航；
- 偏航后是否拉得回来；
- 最终结果是否可验证。

#### 第二，把“智能”变成“可管理资产”

一旦有了角色分工、验证闭环、权限边界、Fallback 链路，AI 不再只是一个不可控的黑盒，而是一个可被治理的工作系统。

#### 第三，允许组织把经验沉淀为结构

一个好的 Harness 会把经验写进：

- 规则；
- 路由；
- 工具描述；
- 校验脚本；
- 工作流节点；
- 角色职责。

这意味着“品味”不再只存在于某个高手脑子里，而会慢慢变成系统能力。

#### 第四，给用户更多主权

当模型越来越通用，真正重要的不是“你能不能调用一个大模型”，而是：

- 你能不能决定它何时调用什么；
- 你能不能控制它接触什么信息；
- 你能不能让它在你的规则里工作；
- 你能不能知道它做过什么。

这就是所谓的 AI 主权，更准确一点说，是**AI 编排主权**。

### 5.2 但它也很容易滑向另一种过度设计

讲到这里，也必须泼一点冷水。

Harness Engineering 和 oh-my-xx 这种模式，不代表“角色越多越好，流程越复杂越先进”。

它最大的风险有三个：

#### 风险一：把复杂度误当成能力

有些系统看上去很完整：

- 十几个角色；
- 很多层级；
- 大量规则；
- 各种模式切换。

但如果这些结构没有真正提升完成率，只是在增加认知负担，那它就不是 Harness，而是仪式化复杂度。

#### 风险二：过度迷信分类和路由

分类器很重要，但分类器不是上帝。任务世界本来就模糊，很多真实需求跨越多个边界。一个系统如果过度依赖刚性分类，反而会在边缘案例里表现很差。

#### 风险三：让编排系统自己变成新的黑盒

我们原本是为了控制模型黑盒，才引入 Harness。可如果最终连 Harness 自己都变成没人解释得清的巨大规则堆，那其实只是把黑盒换了个位置。

所以真正好的系统，应该始终保留三种品质：

- **可解释**：为什么这样路由、为什么这样验证，能说清；
- **可审计**：做了什么、改了什么、何时失败，能追溯；
- **可裁剪**：小任务可以走轻路径，大任务再上重系统。

### 5.3 最关键的一条判断：Agent 数量不是重点，边界设计才是重点

很多关于多 Agent 的讨论最后都会跑偏，好像系统强不强，取决于你有多少个 Agent。

其实真正重要的不是数量，而是边界。

一个只有单主循环、但边界清晰、验证严格、上下文管理良好的系统，可能比一个“十几个 Agent 同时开会”的系统更可靠。

反过来，一个多代理系统只有在满足下面这些条件时，才会真正产生价值：

- 每个角色的职责足够清晰；
- 上下文彼此隔离；
- 汇报结果足够压缩；
- 主代理始终保有全局判断；
- 验证和审查没有被外包掉。

所以最后的结论并不是“单 Agent 过时了，多 Agent 才高级”，而是：

**当任务复杂度足以让单一上下文失真时，编排系统就需要引入分工；但分工的目标永远是降低复杂度，而不是制造复杂度。**

---

## 总结与展望：从单点模型时代，走向系统智能时代

如果只用一句话概括 Harness Engineering 和 oh-my-xx 这种模式，我会这样说：

> 它们真正推动的，不是“让模型更聪明”，而是“让智能在一个可控系统里持续做对事”。

这背后最重要的变化有三层：

### 第一层：从模型能力，转向运行时能力

未来真正拉开差距的，未必只是模型参数、基准测试或单次推理表现，而是：

- 你如何组织工具；
- 你如何设计权限；
- 你如何管理上下文；
- 你如何设置反馈回路；
- 你如何让系统在失败时保持韧性。

### 第二层：从单点智能，转向系统智能

未来的 AI 产品，越来越像一个由：

- 模型，
- 工具，
- 工作流，
- 规则，
- 验证器，
- 审计与记忆

共同构成的系统。

这里真正的护城河，不只是“你接了哪个模型”，而是“你把这些能力如何组织成一套长期稳定的工作方式”。

### 第三层：从回答问题，转向承担结果

当我们评价一个 Agent 系统时，最应该问的其实不是：

- 它能不能说；

而是：

- 它能不能稳；
- 它能不能审；
- 它能不能改；
- 它能不能复原；
- 它能不能在更复杂的任务里继续保持清醒。

到这里，你会发现 Harness Engineering 最终讨论的，其实不是一个技术热词，而是一种更成熟的 AI 观：

**智能值得追求，但真正稀缺的，从来都是把智能组织起来的能力。**

而 oh-my-xx 这种模式之所以值得关注，也正因为它不是在追求“再多几个 Agent”，而是在试图回答一个更根本的问题：

> 当 AI 不再只是回答机器，而开始变成工作系统时，我们该如何设计它的组织结构、运行纪律和交付闭环？

这才是 Harness Engineering 真正有意思的地方。

---

## 参考资料

1. OpenAI, *Harness Engineering*  
   https://openai.com/zh-Hans-CN/index/harness-engineering/

2. Anthropic, *Building Effective Agents*  
   https://www.anthropic.com/research/building-effective-agents

3. Harness, *AI Deployment in Production: Orchestrating LLMs, RAG and Agents*  
   https://www.harness.io/blog/ai-deployment-in-production-orchestrate-llms-rag-agents

4. Harness, *Introducing the Harness MCP Server*  
   https://www.harness.io/blog/mcp-announcement

5. oh-my-openagent 文档：Overview  
   https://github.com/code-yeongyu/oh-my-openagent/blob/dev/docs/guide/overview.md

6. oh-my-openagent 文档：Features  
   https://github.com/code-yeongyu/oh-my-openagent/blob/dev/docs/reference/features.md

7. oh-my-openagent 文档：Configuration  
   https://github.com/code-yeongyu/oh-my-openagent/blob/dev/docs/reference/configuration.md
